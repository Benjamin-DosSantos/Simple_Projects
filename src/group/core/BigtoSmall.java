package group.core;

import java.applet.Applet;
import java.awt.Color;
import java.awt.Dimension;
import java.awt.Graphics;
import java.awt.Toolkit;

public class BigtoSmall extends Applet{

	static Dimension screenSize = Toolkit.getDefaultToolkit().getScreenSize();
	static int width = (int)(screenSize.getWidth());
	static int height = (int)(screenSize.getHeight());
	
	Color bgcolor = Color.cyan;
	
	public void init(){
		
		this.setBackground(bgcolor);
		this.setSize(width, height);
		
	}
	
	public static void main(String[] args){}
	
	public void paint(Graphics g){
		
		int x = width / 2, y = height - 95, change=8, width = 20, 
				leftshift= change/2, height = 6;

				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);

g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);

g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);

g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);

g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);

g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
	g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);
				
				width = width + change;
				y = y - height;
				x = x - leftshift;
				g.drawLine(x, y, x+width, y);

		
	}
	
}
